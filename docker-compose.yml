version: "3"
services:
  scraper:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      JOBS_DB_PATH: /app/jobs.db
    volumes:
      - ${JOBS_DB_PATH:-./jobs.db}:/app/jobs.db
